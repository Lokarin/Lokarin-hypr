#!/usr/bin/bash

# Importar cores do pywal se precisar
#. "${HOME}/.cache/wal/colors.sh"

# Menu com Rofi
opcao=$(echo -e "Performance\nBateria" | rofi -dmenu -i -p " ")

case $opcao in
  Performance) powerprofilesctl set performance & ;;
  Bateria) powerprofilesctl set power-saver & ;;
esac

